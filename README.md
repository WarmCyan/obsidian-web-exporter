# Obsidian Web Exporter

## Dependencies

* `rg` - ripgrep (for quickly finding all notes/paths with `#public` in them)
* `pandoc` - for converting markdown to html
* `python > 3.9` - for some minor text processing

## Usage

```bash
export.sh [NOTES_PATH] [EXPORT_PATH]
```
